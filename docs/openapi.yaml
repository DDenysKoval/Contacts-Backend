openapi: 3.1.0
info:
  version: 1.0.0
  title: <назва вашого додатку>
  license:
    name: Apache 2.0
    url: http://www.apache.org/licenses/LICENSE-2.0.html
  description: <опис вашого додатку>
tags:
  - name: Contacts
    description: Operations with Contacts
  - name: Auth
    description: Operations with Auth
servers:
  # - url: http://localhost:3000
  - url: https://nodejs-hw-mongodb-3-rexi.onrender.com
paths:
  /contacts:
    post:
      $ref: ../swagger/path/contacts/post.yaml
    get:
      $ref: ../swagger/path/contacts/get.yaml
  /contacts/{id}:
    get:
      $ref: ../swagger/path/contacts/{id}/get.yaml
    patch:
      $ref: ../swagger/path/contacts/{id}/patch.yaml
    delete:
      $ref: ../swagger/path/contacts/{id}/delete.yaml
components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
